[project]
name = "kitchen-companion"
version = "0.1.0"
description = "A local AI recipe chatbot using LangGraph and Chroma."
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]